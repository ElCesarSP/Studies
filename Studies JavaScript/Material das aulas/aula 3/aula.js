/**
 * Usando console.log('');
 * Usaremos para imprimir no console o que estamos programando, 
 * para realizarmos teste para nosso codigo
 * E para testamos
 */

console.log('César Santos');
// Podemos usar aspas simples para nosso codigo so que tem uma falha que
// Nao poderiamos usar nova aspas simples nesse caso deveria usar aspas duplas
console.log(`César Santos`);
// Nesse caso e mais indicar para usarmos para nosso codigo 
// e a forma de pogramação mais efeciente
console.log("César Santos");
// Podemos usar aspas duplas para nosso codigo so que tem uma falha que
// Nao poderiamos usar nova aspas duplas nesse caso deveria usar aspas siples

// Quando Usamos aspas simples, duplas, ou crase são sempre para indicar string

// Agora podemos usar expresssão númuerico  como exemplo 1 2 3 4 5 6 7 8 9 ...

console.log(1,2,3,4,5,6,7,8,9);

// Também podemos usar numerico fluante como (float) 12.3 , 0.56 , 1233.23 

console.log( 12.3 , 0.56 , 1233.23 );

// Podemos usar tipos de dados como expressao numerica, e string juntos 
// Podemo concatenar ou usar ( virgula ) , ou essa expressao ${nome da variavel}
