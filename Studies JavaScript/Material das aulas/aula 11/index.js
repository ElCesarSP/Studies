/**
 * Operadores Aritméticos |
 * 
 *  ( +, -, /, *) |
 * + Adição e Concatenação |
 * - Sublitação |
 * / Divisão |
 * * Mulplicação |
 * ** Potenciação |
 * % Resto da divisão |
 * 
 * Precendencia  ordem de soluções matemáticos |
 * ( ), ** , * / % , + - ,
 * 
 * Operador de incremento ++; lembrando de sempre usar o let |
 * let contador = 1; |
 * contador++; posso usar  (Ele primeiro chama o valor depois volta o valor de incremento) |
 * ++contador; posso usar (Mas primeiro faz a conta e retornar o valor) |
 *  
 * Operador de decremento --; ele vai sublitaria nossa operação |
 * let contador = 1; |
 * contador--;  (Ele primeiro chama o valor depois volta o valor de decremento) |
 * --contador; (Mas primeiro faz a conta de decremento e retornar o valor) |
 * 
 * Operadores de atribuição *=, **= |
 * Os operadores de atribuição em programação são usados para atribuir |
 * valores a variáveis. O operador mais comum de atribuição é o =, |
 * mas existem outros operadores que realizam a atribuição de forma mais |
 * específica, muitas vezes combinando a atribuição com uma operação aritmética ou lógica. |
 * Atribuição simples (=): |
 * Atribuição de soma (+=): |
 * Atribuição de subtração (-=): |
 * Atribuição de multiplicação (*=): |
 * Atribuição de divisão (/=): |
 * Atribuição de divisão inteira (//=): |
 * 
 * Atribuição de módulo (%=): |
 * Atribuição de exponenciação (**=): |
 * 
 * NaN - Not a Number |
 * O termo NaN significa "Not a Number" (em português, "Não é um número"). |
 * Ele é usado em computação para representar um valor que não pode ser representado |
 * como um número válido. |
 * O NaN é frequentemente encontrado em operações que não têm um resultado |
 * matematicamente válido, como: |
 * 
 * Conversao de um String para para expressão numerica |
 * lembrando quando usamos conversão, para string numerica, não poderia ter Letras |
 * que ele so converteria somente números |
 * parseInt('') = Números inteiro |
 * parseFloat('') = Números flutuante |
 * Number('') = Números Reais |
 * 
 */