/**
 * Constante |
 * 
 * Não podemos criar Constante com palavras reservadas |
 * Constante precisam ter nome significativos |
 * Não pode começar o nome de Constante com um numero |
 * Não pode conter espaços ou traços |
 * Case-sensitive |
 * Não pode modificar um valor de uma Constante |
 * 
 * JavaScript e linguagem de tipagem, como saber o dado da variavel o funções ( typeof )
 */

const nome = 'César'; // String

const fristNumber = 5; // Number
const seungNumber = 10;

const conta = fristNumber * seungNumber;
const resultadoDuplicado = conta * 2;
let resultadotriplicado = resultadoDuplicado * 3 ;

console.log(typeof nome);