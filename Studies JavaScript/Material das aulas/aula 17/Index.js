/**
 * Aula usando Math ( Matemática ) |
 * 
 * quando usando Math.floor(e objeto) = vai arrendodar o valor para baixo ;
 * quando usando Math.ceil(e objeto) = vai arrendodar o valor para cima ; 
 * quando usando Math.round(e objeto) = vai arrendodar o valor para mais proximo ; 
 * (Math.max(1,2,3,4,-5123,1234,12,234,)) = ele volta o maior número de um sequencia; = 1234
 * (Math.min(1,2,3,4,-5123,1234,12,234,)) = ele volta o maior número de um sequencia; = -5123
 * (Math.random()) = ele vai gera numeros aleatório
 * Math.random() * (10 - 5)  + 5;
 */

let num1 = 9.54578;

let num2 = Math.floor(num1);