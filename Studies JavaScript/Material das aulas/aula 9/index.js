/**
 * Diferença de var e let |
 * 
 * Umas das diferença que seria do Var |
 * que podemos redeclara a nossa variavel isso não da boa praticas usada na |
 * programação o padrão seria usando o let |
 * ex: var nome = 'Cesar' / var nome = 'Paulo' |
 * numca cria variavel sem a sintax const ou let |
 * 
 */

var nome = 'Cesar';
var nome = 'Luiz'; 

console.log(nome); // Luiz